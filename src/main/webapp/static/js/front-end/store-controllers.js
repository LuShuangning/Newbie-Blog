var storeCtrls = angular.module('storeCtrls', [ ]);

storeCtrls.controller('bookCtrl',['$scope',
	function($scope) {
		
}]);

storeCtrls.controller('softwareCtrl', ['$scope',
	function($scope){
		
}]);